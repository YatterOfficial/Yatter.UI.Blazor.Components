@using ListBuilder = Yatter.UI.ListBuilder.ListItems;

<div style="visibility:@CastItem?.Visibility;color:@CastItem?.TextColor;margin:@CastItem?.Margin;padding:@CastItem?.Padding;"><h2>@CastItem?.Text</h2></div>

@code {

    private object? _item;
    [Parameter]
    public object? Item
    {
        get
        {
            return _item;
        }
        set
        {
            _item = value;

            if (Item != null)
            {
                CastItem = (ListBuilder.H2)Item;
            }
        }
    }

    public ListBuilder.H2? CastItem { get; set; }
}
