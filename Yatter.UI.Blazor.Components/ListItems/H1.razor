@using ListBuilder = Yatter.UI.ListBuilder.ListItems;

<div style="visibility:@CastItem?.Visibility;color:@CastItem?.TextColor;margin:@CastItem?.Margin;padding:@CastItem?.Padding;"><h1>@CastItem?.Text</h1></div>

@code {

    private object? _item;
    [Parameter]
    public object? Item
    {
        get
        {
            return _item;
        }
        set
        {
            _item = value;

            if (Item != null)
            {
                CastItem = (ListBuilder.H1)Item;
            }
        }
    }

    public ListBuilder.H1? CastItem { get; set; }
}
